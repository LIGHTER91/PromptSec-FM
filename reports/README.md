# Generated reports

Build and validation reports are written here and are ignored by Git. A build report
records the source revision, record count, mapping statuses, verdict counts, and the
SHA-256 digest of the canonical JSONL output.

